// .sequelizerc

const { resolve } = require("path");

module.exports = {
  config: resolve("src/config", "sequelizeConfig.js"),
  "models-path": resolve("src", "models"),
  "seeders-path": resolve("db", "seeders"),
  "migrations-path": resolve("db", "migrations"),
};
